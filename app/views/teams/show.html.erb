<h1><%= @team.name %></h1>

<p><%= @team.team_score %></p>

<!-- find team members where user.team_id = team_id -->

<ul>
  <%= User.all.each do |user| %>
    <% if user.team_id == @team.id %>
        <li><%= user.name %></li>
    <% end %>
  <% end %>
</ul>
